services:
  ttt-lisp:
    image: sbcl-quicklisp:1.0
    container_name: ttt-lisp
    working_dir: /root/quicklisp/local-projects/
    volumes:
      - ./repos/ttt:/root/quicklisp/local-projects/ttt
    tty: true
    stdin_open: true
    command: bash
